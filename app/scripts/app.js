
// Gets weather info from forecast.io API
$( document ).ready(function() {
  
});

// Renders forecast.io JSON into HTML

$(document).ready(function() {
    //render weather icon based on current conditions
    function() {
       //rainy
       
       //cloudy
       
       //sunny
       
       //overcast
       
       //partly cloudly
       
      
    }


});

// Logic for if it's summer or not
$(document).ready(function() {
   
    //variables 

    var d = newDate();  //gets date object 
    var month = d.getMonth();  // 0 to 11,  0=Jan, 11=December. 
    var day = d.getDay(); //

    var temperature = temp info from json

    var date = month.concat(day);  //combines month and date into single numeric string

    //check if date between 522 and 820

    if (date >=522 && date < 820 && temperature > 64)  {
    //yup seems like summer
    };

    else if (date >=522 && date < 820 && temperature < 64)  {
    //lol it may be summer but it sure don't feel like it right now
    }

    else if (date >= 820 && date <= 1131) {
    //awww hell nah it's fucking cold out man.


    else (date < 522) {
    }
   
});